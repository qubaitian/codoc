Read `CONTEXT.md` and the ADRs in `docs/adr/` before exploring.
Write code in `CODOC.md`.  

One idea per sentence.  
One sentence per line with two spaces.  

Name every function using `new-*`, `del-*`, `set-*`, `get-*`, `if-*`, `test-*`.  
Each function must belong to exactly one operation category.  

Use Conventional Commits for commit messages.  
Use Conventional Branch Name.  

Before commit, run `./codoc del`
