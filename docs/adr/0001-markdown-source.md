# Use Markdown as the sole authoring source

Codoc treats one Markdown document as the only authored source.
The tool generates target files from marked code blocks.
We choose this model to keep explanations and code together.
This makes generated files reproducible from one editing surface.
